package com.swarawan

object AppConfiguration {
	const val REGION_ID = "{your-region-id}"
	const val ACCESS_KEY = "{your-ram-access-key}"
	const val ACCESS_SECRET = "{your-ram-secret-key"

	/**
	 * This constant is not used in application.
	 * In case you have your Key Id you can store to below field just to remember,
	 * so no need to use Find All Key API anymore
	 */
	const val KMS_KEY_ID = "{you-can-ignore-this}"
}